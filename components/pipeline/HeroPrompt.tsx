'use client';

/**
 * HeroPrompt — DEPRECATED.
 * All hero UI has been merged into PipelineView (CHANGE 2: User Flow Redesign).
 * Kept as a re-export for any remaining imports.
 */
export function HeroPrompt() {
  return null;
}
